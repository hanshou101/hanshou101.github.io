# .frac.md — public

> 生成规则：本文件只总结当前目录的直属文件，以及直属子目录的 `.frac.md`。
> 最近更新：2026-08-31 00:15
> 输入范围：direct-files + child-fracs only

## 1. 本目录是什么

静态资源根：全部为 AI 生成或可商用占位媒体，按用途分四个子目录。standalone 部署时需随包拷贝（deploy-cloud.sh 已处理），Pages 部署自动带上（注意 basePath）。

## 2. 直属文件

无直属文件（媒体均在子目录）。

## 3. 子目录导航

- `covers/`：见 `covers/.frac.md`；六剧海报封面（Seedream）。
- `frames/`：见 `frames/.frac.md`；分集初始帧（每剧 9 张）。
- `samples/`：见 `samples/.frac.md`；套装 A 占位样片 + TTS 配音。
- `samples-live/`：见 `samples-live/.frac.md`；套装 B 真实生成产物（含 d1 正片）。

## 4. 本层约束 / 约定 / 禁忌

- 媒体文件名即前端协议，重命名必须同步 `src/lib/samples.ts` / `src/components/Poster.tsx`。
- 总体积控制意识：均为 480p/压缩产物，git 直存（当前约 60MB 级）。

## 5. 什么时候需要下钻

- 找某资源的消费方：先查 `src/lib/samples.ts` 的映射。
