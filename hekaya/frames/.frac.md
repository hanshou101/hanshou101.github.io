# .frac.md — public/frames

> 生成规则：本文件只总结当前目录的直属文件，以及直属子目录的 `.frac.md`。
> 最近更新：2026-08-31 00:15
> 输入范围：direct-files + child-fracs only

## 1. 本目录是什么

观众端分集初始帧：d1-d6 每剧 9 张 Seedream 生成场景图（按集数轮换作播放背景）；d7-d9 从合作方成片逐集截取（与分集一一对应，兼作播放器 poster 防黑屏）。

## 2. 直属文件

- `d{1..6}-{1..9}.jpg`：Seedream 生成，命名 `<dramaId>-<n>.jpg`，映射在 `src/lib/samples.ts` 的 EPISODE_FRAMES（framesN 函数）。d4 全套为拳击题材（男性题材改版后）。
- `d7-{1..5}.jpg` / `d8-{1..6}.jpg` / `d9-{1..5}.jpg`：成片各集中段截帧，与 `public/episodes/` 分集对应。

## 3. 子目录导航

无子目录。

## 4. 本层约束 / 约定 / 禁忌

- 场景需贴每剧剧情且互不重复；遵守与 covers 相同的审核安全约束。
- 扩充每剧张数时同步 `frames9` 的长度参数。

## 5. 什么时候需要下钻

- 播放页轮换逻辑在 `src/components/viewer/PhoneApp.tsx`（epFrame 计算）。
