# .frac.md — public/frames

> 生成规则：本文件只总结当前目录的直属文件，以及直属子目录的 `.frac.md`。
> 最近更新：2026-08-31 00:15
> 输入范围：direct-files + child-fracs only

## 1. 本目录是什么

观众端分集初始帧（Seedream 生成）：每剧 9 张互不重复的剧情场景图，按集数轮换作为无正片集数的播放背景，未来可作各集 first_frame。

## 2. 直属文件

- `d{1..6}-{1..9}.jpg`：命名 `<dramaId>-<n>.jpg`，映射在 `src/lib/samples.ts` 的 EPISODE_FRAMES（frames9 函数按此约定生成路径）。d4 全套为拳击题材（男性题材改版后）。

## 3. 子目录导航

无子目录。

## 4. 本层约束 / 约定 / 禁忌

- 场景需贴每剧剧情且互不重复；遵守与 covers 相同的审核安全约束。
- 扩充每剧张数时同步 `frames9` 的长度参数。

## 5. 什么时候需要下钻

- 播放页轮换逻辑在 `src/components/viewer/PhoneApp.tsx`（epFrame 计算）。
